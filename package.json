{
	"name": "part3",
	"version": "1.0.0",
	"description": "Fullstackopen part3",
	"main": "index.js",
	"scripts": {
		"start": "node index.js",
		"dev": "nodemon index.js",
		"build:ui": "cd ../part2/phonebook && pnpm run build:ui",
		"build:server": "rm -rf dist && pnpm run build:ui && cp -r ../part2/phonebook/dist ./",
		"deploy": "pnpm run build:server && git add . && git commit -m serverbuild && git push"
	},
	"keywords": ["express", "node"],
	"author": "mellambias@gmail.com",
	"license": "ISC",
	"dependencies": {
		"cors": "2.8.5",
		"express": "4.19.2",
		"morgan": "1.10.0"
	},
	"devDependencies": {
		"nodemon": "3.1.4"
	}
}
